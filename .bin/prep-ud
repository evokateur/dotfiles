#!/bin/bash

if [ "$(id -u)" -ne 0 ]; then
    echo 'script must be run by root' >&2
    exit 1
fi

echo "telling dropbox to quit.."
osascript -e 'tell application "Dropbox" to quit'

echo "starting bclm-cc script.."
bclm-cc
